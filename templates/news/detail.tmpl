<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ .Title }}</title>
    <link rel="stylesheet" href="/static/css/main.css">
</head>
<body>
    {{ template "layout.tmpl" . }}

    <div class="news-detail">
        <h1>{{ .PostLink.Title }}</h1>
        <p>来源: {{ .PostLink.Source }}</p>
        <p>发布时间: {{ .PostLink.PublishedAt.Format "2006-01-02 15:04:05" }}</p>
        <div class="content">
            {{ .PostLink.Content }}
        </div>
        <a href="{{ .PostLink.URL }}" target="_blank">阅读更多</a>
    </div>

    <script src="/static/js/main.js"></script>
</body>
</html>